<%- include('../includes/head.ejs') %>
</head>

<body>
  <%- include('../includes/navigation.ejs') %>
  <main>
    <% if (products.length > 0) {%> 
        <ul>
            <% products.forEach(p => { %> 
                <li> 
                    <p><%=p.productData.title %> (<%= p.qty %>)</p>
                    <form action="/cart-delete-item" method="post">
                        <input type="hidden" value = "<%=p.productData.id%>" name="productId">
                        <button class ="btn" type ="submit">Delete</button>
                    </form>
                </li> 
            <%}) %>
        </ul> 
        <hr>
        <div style="font-family: 'Courier New', Courier, monospace; font-size: 30px ; border: 1px solid rgb(0, 132, 35); width: auto;">Cart Total : <%=cartTotal%></div>
    <% }  else { %> 
        <h1>No Products in the Cart !</h1>
    <% } %>
  </main>
  <%- include('../includes/end.ejs') %>